Procedural storyboard video runtime marker.

This directory exists so the local video manifest can resolve to an installed runtime
without downloading a heavyweight text-to-video checkpoint. The current implementation
generates animated gif storyboard previews locally.
